<div class="row formulario">
  <div class="col s12 m8 offset-m2">
    <div class="card">
      <div class="card-content">
        <form {{action "cadastrarSetor" on="submit"}}>
          <div class="row">
            <h4>Novo Setor</h4>
            <div class="input-field col s12">
              <label>Nome do setor</label>
              {{input id="inputNome" class="nome" value=nome required=true maxlength=33}}
            </div>
          </div>
          <div class="row">
            <div class="col s12 right-align">
              <button type="submit" class="salvar btn wave-effects wave-light">Salvar</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

{{modal-confirm idModal="modalConfirmarReativacao" title="Confirmação" content="Já existe um setor inativo com este nome, deseja reativá-lo?" actionConfirm=(action "reativarSetor")}}

{{outlet}}
